# Purpose: Minimal config with a single mixed port. Used for smoke-testing
# basic proxy startup. No proxies defined — all traffic goes DIRECT/GLOBAL.

mixed-port: 8899
external-controller: 127.0.0.1:9090
mode: global
bind-address: "0.0.0.0"
