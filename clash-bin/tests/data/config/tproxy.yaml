# Purpose: Transparent proxy (tproxy) config for Linux tproxy integration tests.
# Requires CAP_NET_ADMIN / root and Linux kernel tproxy support.

tproxy-port: 8900

mode: rule
log-level: debug

rules:
  - MATCH, DIRECT
